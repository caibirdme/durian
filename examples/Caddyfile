:8051 {
    proxy {
        path /foo
        upstream {
            localhost:9001
            localhost:9002
        }
        max_conn 1000 #keep no more than max_conn connections to upstream
    }
}
